# Core dependencies (install first for build requirements)
numpy>=1.26.0,<2.0
Cython>=0.29
scikit-learn>=1.4.0
scipy>=1.11.0

# Matplotlib - PyCaret 3.3.x requires <3.8.0 for yellowbrick compatibility
matplotlib>=3.7.0,<3.8.0

# Main packages
pycaret>=3.3.0
polars>=1.0.0
pandas>=2.1.0,<2.2

# Jupyter
ipykernel>=6.29.0
notebook>=7.0.0
ipywidgets

# Utilities
kaggle>=1.6.0
tqdm>=4.66.0
seaborn>=0.13.0
pyarrow>=15.0.0

# Additional packages required by the enhanced PyCaret notebook
# - optuna: used as the search_library for tune_model(..., search_library='optuna')
# - plotly: optional plotting backend used by some PyCaret plots
# - shap: feature explanation used by some diagnostic plots and analysis
optuna>=3.2.0
plotly>=5.0.0
shap>=0.41.0

# Recommended explicit versions for tree boosters to avoid binary incompatibilities
lightgbm>=3.3.5
xgboost>=2.0.1
catboost>=1.2.2